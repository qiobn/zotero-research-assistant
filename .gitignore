# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
*.egg
.venv/

# Environment / credentials (contain API keys & tokens)
.env
.pypirc*

# IDE
.vscode/
.idea/
.cursor/
*.swp
*.swo

# Claude Code: keep local config (settings, worktrees) ignored, but TRACK skills/
.claude/*
!.claude/skills/
!.claude/skills/**

# ChromaDB / Vector store
.chroma_db/
.chroma_ablate_*/

# Sync state + retrieval logs (generated per-machine, inside .chroma_db/)
_sync_state.json
_retrieval_log.jsonl
_retrieval_log.idx

# User-defined query synonyms (per-machine)
query_dict_user.json

# Evaluation baseline (per-machine, depends on local library content)
tests/eval_baseline.json

# Ruff
.ruff_cache/

# uv
.local/
uv.lock

# pytest
.pytest_cache/
htmlcov/
.coverage

# Docs (internal guides, not for distribution)
docs/internal/

# Development progress / roadmap (internal)
DEVELOPMENT.md
ARCHITECTURE.md

# OS
.DS_Store
Thumbs.db
